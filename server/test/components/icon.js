// Commented sample test case to resolve lint error.

// import React from 'react';
// import { expect } from 'chai';
// import Enzyme, { mount } from 'enzyme';
// import Adapter from 'enzyme-adapter-react-16';
// import Icon from '../../src/components/icon';

// Enzyme.configure({ adapter: new Adapter() });

// describe('Icon component', () => {
//   const icon = mount(<Icon cssName='user-small' />);

//   it('should have cssName props with value equals to "user-small"', () => {
//     expect(icon.props().cssName).to.equals('user-small');
//   });

//   it('should render class "icon-user-small"', () => {
//     expect(icon.find('.icon-user-small').length).to.equals(1);
//   });
// });
